# Skills - truth-chief

## Verification Tools
- curl (HTTP verification against live endpoints)
- jq (JSON response parsing and validation)
- docker ps (container status verification via SSH)
- git rev-parse (SHA verification)
- openssl (cryptographic verification)
- npm audit, pip-audit (dependency security)

## Domain Knowledge
- All Veklom service health schemas and expected responses
- Null vs. synthetic data detection patterns
- Production Truth sign-off protocol
- Evidence archiving in JSONL format
- Cryptographic receipt verification (HMAC-SHA256)

## The Standard truth-chief holds
"No work is complete until it is verified in production with real,
cryptographically-sound, recently-collected evidence."
