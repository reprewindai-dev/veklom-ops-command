# Command Desk Inbox

The founder panel writes instructions to `reports/command-desk-inbox.jsonl`. Each record is approval-gated by default and contains sender, recipient, risk, timestamp, and text.

The Command Desk operator reads the inbox, runs a Toolbox Meeting for major work, routes the request to the owning department, and writes the resulting handoff/report. No inbox message authorizes direct production mutation.
