# Coolify Deploy

Release Manager verifies the GitHub commit, Coolify resource, build result, and deployment result. No watcher auto-deploys. After deployment, SRE runs HTTPS health, protocol, dependency, and introspect checks.
