# Protocol JSON Standard

Public manifests must validate against `standards/protocol.schema.json`. Never publish secrets, container names, private IPs, SSH paths, or fabricated capabilities. VNP capability names are `vnp-staking`, `sla-bonds`, `settlement-evidence`, and `telemetry-scoring`.
