# Rebuild Survival Check

From a clean checkout of the owning repo, install locked dependencies, run the documented build and tests, and confirm protocol/health routes are source-backed. A local build never substitutes for deployment proof.
