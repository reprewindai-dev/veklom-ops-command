# Build / IDE Agent Roster

Jean Workspace Agent opens repos and produces safe task packets. Poltergeist Watcher owns daemon lifecycle. Windsurf and Cursor Helpers adapt the same contract. Codex Task Writer produces deterministic prompts. GitHub MCP Repo Agent handles repository context.

The source contract is IDE-neutral: repo, branch, acceptance checks, changed files, and handoff evidence.
