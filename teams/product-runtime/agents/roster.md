# Product Runtime Agent Roster

Governance Portal Agent owns the control shell. Apex Agent keeps static boundaries explicit. GPC Canvas Agent maps governance state. PiP Terminal Agent consumes protocol contracts. Human/Machine Toggle Agent guards execution UX behind Cappo permits.

No agent may enable live execution without permit enforcement and evidence hooks.
